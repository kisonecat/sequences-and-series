<% require './course' %>

<%= week_title( 2, "Series" ) %>

<p id="week-summary">In this second week, we introduce the second main topic of study: series.</p>

<p>Material for week two will be posted around 10:00am Eastern on <%= WEEK_STARTS[1].strftime('%B %e, %Y').strip %>.</p>

<h4>What is a sequence?</h4>

<%= week_pager(2) %>
